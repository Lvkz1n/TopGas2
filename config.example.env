# Configurações do TopGas
# Copie este arquivo para .env e ajuste as configurações conforme necessário

# Ambiente (development para localhost, production para Easy Panel)
NODE_ENV=development

# Para forçar modo localhost mesmo com NODE_ENV=production
LOCALHOST=true

# Configurações do servidor
PORT=3000
CORS_ORIGIN=http://localhost:3000
SESSION_SECRET=topgas_secret_key_2025
SERVE_STATIC=true

# Configurações do banco de dados (para localhost)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=topgas
DB_USER=postgres
DB_PASSWORD=postgres

# URL completa do banco (opcional, sobrescreve as configurações acima)
# DATABASE_URL=postgres://postgres:postgres@localhost:5433/topgas?sslmode=disable

# Configurações para Easy Panel (produção)
# NODE_ENV=production
# PORT=8080
# CORS_ORIGIN=*
# SERVE_STATIC=false
# DATABASE_URL=postgres://postgres:postgres_cerion@2025@166.0.186.92:5432/postgres_rabbitmq?sslmode=disable
